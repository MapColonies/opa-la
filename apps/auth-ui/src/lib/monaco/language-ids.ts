/**
 * The language ids, apart from the registration module, so a page can name a language
 * without importing the editor library and everything it drags in.
 */
export const REGO_LANGUAGE_ID = 'rego';

/** Rego with template interpolations highlighted; used for a template asset. */
export const REGO_TEMPLATE_LANGUAGE_ID = 'rego-template';
